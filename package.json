{
  "name": "hcmute-course-registrar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd web-app && npm install --include=optional --include=dev && npm run build",
    "start": "npx -y serve -s web-app/dist -l ${PORT:-8080}"
  }
}
