{
  "name": "hcmute-course-registrar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd web-app && NPM_CONFIG_PRODUCTION=false npm install --include=dev && npm run build",
    "start": "npx -y serve -s web-app/dist -l ${PORT:-8080}"
  }
}
