{
  "name": "hcmute-course-registrar",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web-app"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "npm run build --workspace web-app",
    "start": "serve -s web-app/dist -l tcp://0.0.0.0:${PORT:-8080}"
  },
  "dependencies": {
    "serve": "^14.2.5"
  },
  "overrides": {
    "lightningcss": "1.33.0"
  }
}
